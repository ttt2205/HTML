Titile: Units and Sizes
    1.  px
        rem: phụ thuộc vào px mặc địng hoặc trong cùng phần tử
        em: phụ thuộc vào rem trong cùng phần tử


Titile: Typography
=> Typography is the way that text is arranged and presented
    1. inherit : giá trị kế thừa từ phần tử cha
    -> vd: font: inherit
    2. text-decoration
    3. text-transform
    4. text-align
    5. line-height: 1.5; 
    6. font-style


Title: Styling links
    1. thứ tự cài đặt lớp giả cho thẻ a:
        visited - hover, focus - active
    2. opacity: độ trong suốt giá trị từ (0-1)
    3. ::marker: phần tử giả
    -> áp dụng cho các phần tử đã được đánh dấu (các phần tử trong list-item)
    
